import numpy as np

arr = np.array([[[1, 2, 3], [4, 5, 6]]])
print(arr)
#a
numOfRows = np. size(arr, 0)
print("rows:",numOfRows)
numOfColumns = np. size(arr, 1)
print("columns:",numOfColumns)
#b
print("Dimension",arr.shape)
#c
rs=np.reshape(arr, (3, 2))
print(rs)